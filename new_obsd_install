pkg_add wget git firefox ImageMagick nmap rsync vim figlet zsh xplanet avr-binutils avr-libc avr-gcc avr-gdb bzip2 curl magicpoint texlive_texmf-full zip ssvnc

# add to /etc/rc.conf.local
# ntpd_flags="-s" 
# apmd_flags="-A"
# rdate_flags="-n pool.ntp.org"

echo "machdep.lidaction=0" >> /etc/sysctl.conf
