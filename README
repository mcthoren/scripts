just a collection of small simple shell scripts i wind up using often.

TODO:
+ rpi_initial_setup.yml
  - ganglia conf doesn't comment out multicast with a fresh file
  - remove dyn stuff from nsswitch
  - can i assign a host name from ansible based on ip?
  - debug why i have to set a pass for a user before i log in with a key (did this once, forgot)
  - figure a way to do figlet motd's w.o. a change popping up each time
  - set timezone with ansible
  - set locales with ansible
  - expand fs with ansible
